apiVersion: v1
kind: Service
metadata:
  name: large-sized-data-server--service
  namespace: default
  labels:
    app: large-sized-data-server--service
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: large-sized-data-server--100mb
  type: ClusterIP
