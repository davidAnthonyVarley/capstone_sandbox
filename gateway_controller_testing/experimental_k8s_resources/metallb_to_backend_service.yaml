apiVersion: v1
kind: Service
metadata:
  name: backend-lb
  namespace: default
  annotations:
    metallb.universe.tf/address-pool: default-ip-addresses-pool
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
  - name: http
    protocol: TCP
    port: 3000
    targetPort: 3000
