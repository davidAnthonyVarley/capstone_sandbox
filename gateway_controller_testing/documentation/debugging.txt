1. Do you find that when you run the https 1.1 microservice, you visit the url (e.g.  http://127.0.0.1:61449) provided by 'minikube service <service name>' but it doesn't work?

Fix: Change http to https, and 127.0.0.1 to localhost. Proceed even if it tells you that the site is unsafe

2. Does it tell you "SVC not found" despite there clearly being a command to create a new service in manageK8sResources.ps1?

Fix: in your microservices folder (e.g. microservices\https-1p1-helloworld\), check if the Dockerfile 